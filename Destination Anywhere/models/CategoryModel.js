class CategoryModel {
  constructor(categoryId, categoryName, itemList) {
    this.categoryId = categoryId;
    this.categoryName = categoryName;
    this.itemList = itemList;
  }
}

module.exports = CategoryModel;